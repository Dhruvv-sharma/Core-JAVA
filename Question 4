//Write a Java program to enter base and height of a triangle and find its area. A = 1/2 × b × h.
import java.util.Scanner;
public class TriangleArea {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		System.out.println("Enter Base of a Triangle.");
		int base = sc.nextInt();
		System.out.println("Enter Height if a Triangle.");
		int height = sc.nextInt();
		System.out.println("Area is : "+(base*height/2));
	}
}
